1) Ќа кликнутую ссылку вешать класс current. ѕри клике на другую снимать класс с текущей и переносить на кликнутую.
2)—крипт сделать скорее всего с помощью цикла each(). 
	а)¬ешать на каждый элемент меню обработчик событи€
	б)ќтмен€ть действие по умолчанию с <a>
	в)получать текущий url  записывать в переменную var a = this.href дл€ .load()
	г)—корее всего сделать проверку switch-case что бы в зависимости от event.target выбирать с каким id блок будет загружатьс€ в область и сохран€ть в переменную  в каждом case
	выполн€ть определенный .load() с опр. параметрами и брейкать цикл break
	
	
3)ѕодобрать нормальные анимации по€влени€ блоков подгружаемых с Ajax
4)—верстать все страницы дл€ загрузки с помощью Ajax